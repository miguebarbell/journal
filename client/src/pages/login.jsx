import styled from "styled-components";

const Container = styled.div``;
const Login = () => {
    return (
        <Container>
            Login component
        </Container>
    );
};

export default Login;
