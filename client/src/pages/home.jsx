import styled from "styled-components";

const Container = styled.div`
  width: 100vw;
  height: 100vh;
  background-color: green;
`;
const Home = () => {
    return (
        <Container>
            Home component
        </Container>
    );
};

export default Home;
